interface:
  display_name: "X-Wiki Compiler"
  short_description: "Compile private evidence into a durable public-safe wiki"
  default_prompt: "Use $x-wiki-compiler to merge new raw sources into the durable X-Wiki knowledge layer."
