[tool.ruff]
target-version = "py310"
line-length = 100
extend-exclude = ["sites/.site-docs", "sites/site"]

[tool.ruff.lint]
select = ["B", "C4", "E", "F", "I", "RUF", "SIM", "UP"]

[tool.pytest.ini_options]
addopts = "-q"
testpaths = ["scripts/tests"]
